/**
 * 1. what is my character set
 * 2. how many chars are there?
 */

const isStrHavingUniquePChar = () =>{

}

const str = "abcdegfg";
const ALPHABET_COUNT = 128;

if (str.length > ALPHABET_COUNT) {
    /retimp
}

const flags = (new Array(128)).fill(false);
